{
  "name": "designref-local-prototype",
  "version": "0.1.0",
  "description": "Local-first MVP for collecting UI screenshots and generating reusable design prompts.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check api/analyze-image.js && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4173"
  }
}
